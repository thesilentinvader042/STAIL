// vitest setup file
// Once vitest is installed (npm install -D vitest jsdom @testing-library/react @testing-library/jest-dom),
// this file will run before each test suite.

// @ts-ignore – installed by vitest package
import '@testing-library/jest-dom';
